package com.example.mysimplegame.fragments.gamedetails

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class GameViewModule(application: Application) : AndroidViewModel(application) {
}