<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MySimpleGame" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@color/background_light</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <style name="MyToggleButtonStyle" parent="android:Widget">
        <item name="android:background">@drawable/btn_toggle_bg</item>
        <item name="android:textOn"/>
        <item name="android:textOff"/>
        <item name="android:disabledAlpha">?android:attr/disabledAlpha</item>
    </style>
    <style name="MyButtonStyle" parent="android:Widget">
        <item name="cornerRadius">10dp</item>
        <item name="background">@drawable/btn_bg</item>
        <item name="android:width">250dp</item>
    </style>

</resources>